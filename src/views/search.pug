extends base
include mixins/video

block content
  form(method="GET")
    input(name="search_query", type="text", placeholder="Search Video by Title")
    input(type="submit", value="Search")

  each video in videos
    +video(video)
